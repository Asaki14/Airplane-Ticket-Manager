---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/app/page.tsx
  - src/components/deals/DealCard.tsx
  - src/components/deals/DiscoveryPreferences.tsx
  - src/components/deals/CompareAndSavePanel.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The main page layout feels spacious, professional, and structured like a modern OTA (e.g., Ctrip/Fliggy)"
    - "Flight deal cards have ample breathing room, clear typography, and professional visual hierarchy"
    - "The overall UI uses appropriate negative space and subtle container styling to avoid feeling 'too plain' or cramped"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Professional page container with proper sectioning and spacing"
    - path: "src/components/deals/DealCard.tsx"
      provides: "OTA-grade flight card UI with improved grid and typography"
  key_links: []
---

<objective>
Optimize the UI appearance and layout to transform it from a plain, cramped look into a highly professional OTA platform aesthetic (inspired by Ctrip, Fliggy).
Purpose: Enhance user trust and visual hierarchy by applying `frontend-design` and `ui-ux-pro-max` scale principles (better spacing, professional typography, distinct modular sections).
Output: Updated main page layout, redesigned DealCard, and synchronized sub-panels.
</objective>

<execution_context>
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/app/page.tsx
@src/components/deals/DealCard.tsx
@src/components/deals/DiscoveryPreferences.tsx
@src/components/deals/CompareAndSavePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DealCard.tsx for OTA-grade Professionalism</name>
  <files>src/components/deals/DealCard.tsx</files>
  <action>Refactor the card layout applying `ui-ux-pro-max` principles. Increase internal padding (e.g., p-5 or p-6). Structure the flight info (origin, destination, times) using a clear grid or flex layout with a visual connector (like an arrow or flight icon) between origin and destination. Make the price highly prominent (larger, primary color) and align it cleanly. Use subtle borders and modern soft shadows (`shadow-sm` hovering to `shadow-md`) to define the card boundaries without being harsh. Ensure tags (rules, baggage) are elegantly styled and not cluttered.</action>
  <verify>
    <automated>npm run lint -- src/components/deals/DealCard.tsx</automated>
  </verify>
  <done>Deal cards look spacious, information is easily readable at a glance, and visual hierarchy resembles top-tier travel apps.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Global Page Layout and Spacing</name>
  <files>src/app/page.tsx</files>
  <action>Apply `frontend-design` principles to the main page structure. Introduce a subtle off-white or light gray background to the overall page body/main container (e.g., `bg-slate-50` or `bg-gray-50`) so that white cards pop professionally. Increase the gap between cards in the grid/list. Add generous top and bottom padding to major page sections to give the layout breathing room. Ensure the max-width container is well-centered and creates a structured column for the content.</action>
  <verify>
    <automated>npm run lint -- src/app/page.tsx</automated>
  </verify>
  <done>The page has clear section definitions, cards have room to breathe, and the contrast between the page background and card surfaces creates a professional depth.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Discovery and Compare Panels</name>
  <files>src/components/deals/DiscoveryPreferences.tsx, src/components/deals/CompareAndSavePanel.tsx</files>
  <action>Update the styling of the preferences and compare panels to match the newly established professional OTA aesthetic. Align their padding, border-radius, background colors, and typography scales with the new DealCard. Ensure spacing between elements inside these panels is increased and structured cleanly, removing the "cramped" feeling.</action>
  <verify>
    <automated>npm run lint -- src/components/deals/DiscoveryPreferences.tsx src/components/deals/CompareAndSavePanel.tsx</automated>
  </verify>
  <done>Secondary panels match the high-quality visual language of the main cards, maintaining consistency across the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Rendering aesthetic changes, no new trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | UI Components | accept | Purely visual and layout changes, no backend or state logic modified. |
</threat_model>

<verification>
- View the home page and verify the layout feels spacious.
- Check DealCard rendering to ensure clear origin->destination hierarchy and prominent price.
- Confirm background contrast exists between the page and the cards.
</verification>

<success_criteria>
- The UI matches the requested professional, spacious OTA aesthetic (like Ctrip/Fliggy).
- Elements are no longer cramped; padding and margins are generously applied.
- All modified files pass linting without errors.
</success_criteria>

<output>
After completion, the frontend will display a markedly more professional and trustworthy appearance.
</output>
---
phase: quick-260412-hj2-flight-card-advice-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dealAdvice.ts
  - src/components/deals/CompareAndSavePanel.tsx
  - src/styles/globals.css
  - tests/deal-advice.test.ts
  - tests/design-contract.test.ts
autonomous: true
requirements:
  - COMP-03
  - COMP-04
  - PREF-03
must_haves:
  truths:
    - "用户在比较/收藏卡片里可直接看到该航班的特点与购买建议，不再只有按钮和链接"
    - "建议文案展示位置固定在航班标题下方、操作按钮上方，移动端和桌面端都清晰可读"
    - "当命中特定航线（如深圳-香港）时可显示定制建议，未命中时显示通用建议"
  artifacts:
    - path: "src/lib/dealAdvice.ts"
      provides: "航班建议文案生成与兜底规则"
      exports: ["buildDealAdvice"]
    - path: "src/components/deals/CompareAndSavePanel.tsx"
      provides: "比较/收藏卡片中的建议文案渲染位置与语义标签"
      contains: "compare-card-shell__advice"
    - path: "src/styles/globals.css"
      provides: "建议文案排版和卡片中段视觉层级"
      contains: ".compare-card-shell__advice"
    - path: "tests/deal-advice.test.ts"
      provides: "建议生成逻辑测试"
      contains: "buildDealAdvice"
  key_links:
    - from: "src/components/deals/CompareAndSavePanel.tsx"
      to: "src/lib/dealAdvice.ts"
      via: "按 deal 字段生成建议文案"
      pattern: "buildDealAdvice\(deal\)"
    - from: "src/components/deals/CompareAndSavePanel.tsx"
      to: "src/styles/globals.css"
      via: "建议块类名样式命中"
      pattern: "compare-card-shell__advice"
---

<objective>
为“比较、收藏与分享”中的每张航班卡片增加“特点 + 购买建议”文案区，并固定在“航班名”与“加入比较/收藏按钮”之间，提升用户对是否值得立即下单的判断效率。

Purpose: 把“可操作”按钮前置成“先理解再操作”的决策路径，强化信任与转化。
Output: 建议文案生成规则、卡片中段文案渲染、样式与自动化合同测试。
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@src/components/deals/CompareAndSavePanel.tsx
@src/styles/globals.css
@tests/design-contract.test.ts

<interfaces>
From src/components/deals/CompareAndSavePanel.tsx:
```ts
type DealLite = {
  id: string
  title: string
  departureCity: string
  destination: string
  headlinePrice: number
  referenceTotalPrice: number
  valueScore: number
  baggageInfo: string
  refundChangeSummary: string
  stopSummary: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先定义建议文案规则并用测试锁定</name>
  <files>src/lib/dealAdvice.ts, tests/deal-advice.test.ts</files>
  <behavior>
    - Test 1: 深圳→香港且价格在低位区间时，返回包含“性价比高/人少/适合短途出行”语义的建议文案。
    - Test 2: 其他航线返回通用建议（包含价格判断 + 购票动作建议，如“建议尽快下单/可再观察”）。
    - Test 3: deal 关键字段缺失或异常值时，返回安全兜底文案，不抛错。
  </behavior>
  <action>新增 `buildDealAdvice(deal)`：基于 `departureCity/destination/headlinePrice/valueScore/stopSummary` 组合出 1 段 28-50 字的中文建议文案。需内置“深圳-香港”示例规则（与主公描述一致语气），其余航线走通用模板；禁止在组件内硬编码长段文案，统一由该函数返回，便于后续扩展。</action>
  <verify>
    <automated>npm test -- tests/deal-advice.test.ts</automated>
  </verify>
  <done>`buildDealAdvice` 覆盖定制/通用/兜底三类分支，测试全部通过。</done>
</task>

<task type="auto">
  <name>Task 2: 在比较/收藏卡片中插入建议文案并完成中段样式</name>
  <files>src/components/deals/CompareAndSavePanel.tsx, src/styles/globals.css</files>
  <action>在 `CompareAndSavePanel` 的每个 `compare-action-item` 内，将建议文案块插入到标题区后、按钮区前（严格满足主公指定位置）。文案由 `buildDealAdvice(deal)` 提供，并使用语义类名 `compare-card-shell__advice`。在 `globals.css` 增加该类样式：与按钮区保持清晰间距、采用易读正文字号与行高、控制最大行数避免撑爆卡片，且不影响 44px 按钮触达与现有分享链接布局。</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>每张比较/收藏卡片都出现“特点+购买建议”中段，位置和层级稳定，按钮与分享功能不回退。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 增加源码合同测试，防止建议文案区被回退删除</name>
  <files>tests/design-contract.test.ts, src/components/deals/CompareAndSavePanel.tsx, src/styles/globals.css</files>
  <behavior>
    - Test 1: CompareAndSavePanel 源码包含 `compare-card-shell__advice` 渲染钩子。
    - Test 2: globals.css 包含 `.compare-card-shell__advice` 样式定义。
    - Test 3: 组件仍保留“加入比较/收藏/分享链接”关键交互文案。
  </behavior>
  <action>在现有 `design-contract` 套件新增针对比较卡建议文案区的断言，确保后续样式或重构不会把“中段建议”删掉；同时断言核心按钮/分享文案仍存在，避免只保留建议区却丢失操作闭环。</action>
  <verify>
    <automated>npm test -- tests/design-contract.test.ts -t compare</automated>
  </verify>
  <done>合同测试可稳定识别建议区、样式钩子与关键操作文案，回退时可快速失败。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文案规则→用户决策 | 建议文案若误导或过度承诺，会影响用户对价格价值的判断 |
| 样式改动→交互可用性 | 中段新增内容可能挤压按钮触达区或破坏移动端布局 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-hj2-01 | T | src/lib/dealAdvice.ts | mitigate | 文案仅基于可观测字段生成，不输出“保证最低价/保证有票”等绝对承诺语句；异常输入走兜底建议 |
| T-quick-hj2-02 | I | src/components/deals/CompareAndSavePanel.tsx | mitigate | 建议区固定在标题与按钮之间，保留“加入比较/收藏/分享”完整闭环，避免信息误导替代操作入口 |
| T-quick-hj2-03 | D | src/styles/globals.css | mitigate | 对建议区设置行高/最大行数与间距约束，保证按钮最小触达和卡片不溢出 |
</threat_model>

<verification>
- `npm test -- tests/deal-advice.test.ts`
- `npm test -- tests/design-contract.test.ts -t compare`
- `npm run lint`
</verification>

<success_criteria>
- 比较/收藏卡片中，航班名与操作按钮之间稳定出现建议文案区
- 深圳-香港等命中规则航线显示定制建议，其它航线显示通用建议且不报错
- 新增测试通过，且能在建议区被删改时触发失败
</success_criteria>

<output>
After completion, create `.planning/quick/260412-hj2-flight-card-advice-copy/260412-hj2-SUMMARY.md`
</output>

---
phase: 06-ui-ui-phase-front-design-ui-ux-skills-bug
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/deals/DealCard.tsx
  - src/components/deals/CompareAndSavePanel.tsx
  - src/styles/globals.css
  - tests/deal-card-pricing.test.ts
  - tests/preference-and-compare.test.ts
autonomous: true
requirements:
  - PREF-03
must_haves:
  truths:
    - "用户在发现流看到的卡片高度与结构一致，不因文案长短严重跳变"
    - "用户可在比较/收藏区使用一致的卡片结构与按钮交互"
    - "核心决策字段优先展示，次要信息不挤占主卡片阅读空间"
  artifacts:
    - path: "src/components/deals/DealCard.tsx"
      provides: "统一列表卡骨架与摘要截断"
      contains: "deal-card-shell"
    - path: "src/components/deals/CompareAndSavePanel.tsx"
      provides: "比较卡与操作卡统一骨架"
      contains: "compare-card-shell"
    - path: "src/styles/globals.css"
      provides: "列表卡/比较卡一致的结构样式"
      contains: ".compare-card-shell"
  key_links:
    - from: "src/components/deals/DealCard.tsx"
      to: "src/styles/globals.css"
      via: "className -> 骨架类"
      pattern: "deal-card-shell"
    - from: "src/components/deals/CompareAndSavePanel.tsx"
      to: "src/styles/globals.css"
      via: "className -> compare 卡样式"
      pattern: "compare-card-shell"
---

<objective>
完成发现流与比较区的卡片统一化改造：落实统一最小高度、三段骨架、决策优先信息密度，并修复目前卡片高低不齐问题。

Purpose: 直接消除用户最明显的 UI 不一致痛点，提升浏览和比较效率。
Output: DealCard 与 CompareAndSavePanel 的结构改造 + 对应样式和测试更新。
</objective>

<execution_context>
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-RESEARCH.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-01-PLAN.md

<interfaces>
From src/components/deals/DealCard.tsx:
```tsx
type DealCardProps = {
  id: string
  title: string
  departureCity: string
  destination: string
  travelWindowLabel: string
  airline: string
  headlinePrice: number
  referenceTotalPrice: number
  valueScore: number
  publishedAt: string | null
  updatedAt: string | null
  expiresAt: string
}
```

From src/components/deals/CompareAndSavePanel.tsx:
```tsx
type CompareAndSavePanelProps = { deals: DealLite[] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构发现流 DealCard 为统一三段骨架与摘要策略</name>
  <files>src/components/deals/DealCard.tsx, src/styles/globals.css, tests/deal-card-pricing.test.ts</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md, src/components/deals/DealCard.tsx, src/styles/globals.css, tests/deal-card-pricing.test.ts</read_first>
  <action>按 D-01、D-02、D-07 将 DealCard 改为 header/body/footer 三段：header 固定展示 `特价 #id` + 标题链接 + 出发地到目的地；body 固定展示 `travelWindowLabel`、`airline`、`¥headlinePrice`、`参考总成本`、`价值分` 与 freshness 列表；footer 固定 `查看详情与票规` CTA。对标题与可变长文本加摘要截断：标题限制 2 行，正文可变字段限制 2 行并溢出隐藏，避免高度失控。样式层在 `globals.css` 为 `.deal-card-shell` 子元素新增对应规则（含 `min-height: 320px`），并保持主价格 `headlinePrice` 为最显著信息，不改变现有字段语义。</action>
  <acceptance_criteria>
    - `src/components/deals/DealCard.tsx` 包含 `deal-card-shell__header`、`deal-card-shell__body`、`deal-card-shell__footer`
    - `src/styles/globals.css` 包含 `line-clamp` 或等价 2 行截断规则并绑定 DealCard 文本类
    - `tests/deal-card-pricing.test.ts` 仍断言 `headlinePrice` 与 `参考总成本` 共存
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/deal-card-pricing.test.ts</automated>
  </verify>
  <done>发现流卡片结构统一、信息密度符合决策优先、不同文案长度下高度波动受控。</done>
</task>

<task type="auto">
  <name>Task 2: 统一比较/收藏区卡片骨架与交互文案</name>
  <files>src/components/deals/CompareAndSavePanel.tsx, src/styles/globals.css, tests/preference-and-compare.test.ts</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md, .planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md, src/components/deals/CompareAndSavePanel.tsx, src/styles/globals.css, tests/preference-and-compare.test.ts</read_first>
  <action>按 D-02、D-07 将比较区动作卡和比较列卡改为统一骨架类：为列表项 `compare-action-item` 增加 `compare-card-shell`（header 显示标题、body 显示操作按钮组、footer 显示分享链接或状态）；比较列 `compare-column` 使用同源骨架结构并保证字段顺序固定：价格→总成本→行李→退改→经停→价值分。按 UI-SPEC 保持交互文案：`取消收藏`、`移出比较` 不改语义，并在组件内为对应按钮补充 `aria-label`（文案同 destructive confirmation 描述）。更新 `globals.css` 让比较卡最小高度与边框圆角与 DealCard 一致（14px 圆角、统一边框色），并保留 44px 最小触达。</action>
  <acceptance_criteria>
    - `src/components/deals/CompareAndSavePanel.tsx` 包含 `compare-card-shell` 类名
    - `src/components/deals/CompareAndSavePanel.tsx` 中 `取消收藏` 与 `移出比较` 按钮含 `aria-label`
    - `src/styles/globals.css` 中 compare 卡圆角与边框值与 DealCard 保持一致（14px / `#cdd8d1`）
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/preference-and-compare.test.ts</automated>
  </verify>
  <done>比较区在视觉结构、交互触达、字段顺序上与发现流卡片统一。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地收藏状态→UI 展示 | localStorage 数据可能异常，UI 重构不能导致状态误读或误触发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | I | src/components/deals/DealCard.tsx | mitigate | 决策字段顺序固定且主价格突出，避免视觉重构造成信息误导 |
| T-06-04 | T | src/components/deals/CompareAndSavePanel.tsx | mitigate | destructive 按钮增加 aria-label 与固定文案，防止误操作语义丢失 |
</threat_model>

<verification>
- `npm run lint`
- `npm test -- tests/deal-card-pricing.test.ts tests/preference-and-compare.test.ts`
</verification>

<success_criteria>
- 发现流和比较区卡片实现统一骨架与最小高度策略
- 决策优先字段顺序清晰，旧的“卡片高低不齐”问题得到结构性修复
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-ui-ui-phase-front-design-ui-ux-skills-bug-02-SUMMARY.md`
</output>

---
phase: 06-ui-ui-phase-front-design-ui-ux-skills-bug
plan: 06
type: execute
wave: 6
depends_on:
  - 06-05
files_modified:
  - src/components/home/HeroDealCarousel.tsx
  - src/app/page.tsx
  - src/styles/globals.css
  - tests/design-contract.test.ts
  - tests/app-shell.test.ts
autonomous: true
gap_closure: true
requirements:
  - PREF-03
must_haves:
  truths:
    - "用户进入首页后可看到具备层级节奏的轮换卡片区，不再是平铺信息导致氛围感弱"
    - "轮换卡片在移动端与桌面端都可读可操作，并可手动控制播放/暂停"
    - "交互动效节奏在首页核心模块保持一致，且尊重 reduced-motion"
  artifacts:
    - path: "src/components/home/HeroDealCarousel.tsx"
      provides: "首页轮换卡片组件（自动播放、暂停、上一张/下一张、指示器）"
      contains: "aria-label=\"首页特价轮换\""
    - path: "src/app/page.tsx"
      provides: "首页 Hero 区接入轮换卡片并保留既有筛选/列表流程"
      contains: "HeroDealCarousel"
    - path: "src/styles/globals.css"
      provides: "高级感底色层次、轮换卡片布局层级、统一交互节奏"
      contains: ".hero-carousel"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/HeroDealCarousel.tsx"
      via: "组件引入与 props 传递"
      pattern: "HeroDealCarousel"
    - from: "src/components/home/HeroDealCarousel.tsx"
      to: "src/styles/globals.css"
      via: "className 绑定"
      pattern: "hero-carousel|hero-carousel__slide"
---

<objective>
修复 Phase 06 剩余 UAT 闭环缺口（GAP-01 复测项）：以“携程首页轮换卡片”体验为参考，在不引入重型依赖的前提下增强首页氛围感、布局层级和交互质感，并补齐可自动回归的测试合同。

Purpose: 主公已确认详情页/admin 高窄问题关闭，当前仅剩“氛围感不足”；本计划只聚焦首页视觉与交互体验提升，不回退已通过项。
Output: 首页轮换卡片组件 + 高级感底色/层级样式 + 动效一致性与可访问性回归断言。
</objective>

<execution_context>
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-RESEARCH.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-HUMAN-UAT.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-05-PLAN.md

<interfaces>
From src/app/page.tsx:
```tsx
export default async function HomePage({ searchParams }: { searchParams: Promise<SearchParams> })
```

From src/styles/globals.css:
```css
.public-hero { ... }
.public-deal-feed { ... }
```

From tests/design-contract.test.ts:
```ts
expect(publicPage).toContain('public-hero')
expect(publicPage).toContain('查看详情与票规')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现首页轮换卡片组件（可访问 + 移动端适配 + 自动播放可控）</name>
  <files>src/components/home/HeroDealCarousel.tsx, src/app/page.tsx, tests/app-shell.test.ts</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-HUMAN-UAT.md, .planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md, src/app/page.tsx, tests/app-shell.test.ts</read_first>
  <action>按 GAP-01 复测要求并落实 D-02、D-07、D-08：新增 `HeroDealCarousel` 客户端组件，输入为首页前 5 条 deal（复用现有 `mapPublicFeedResponse` 结果，不新增数据源）。组件结构采用“主卡 + 辅助信息”轮换卡片层级，参考携程首页轮换体验但保持本项目文案与业务语义。实现明确交互：`上一张`、`下一张`、`暂停/播放` 按钮、当前索引指示器；默认自动轮播间隔 `4500ms`，用户点击暂停后停止自动轮播；当 `prefers-reduced-motion: reduce` 时默认不自动播放。为可访问性加入 `aria-label="首页特价轮换"`、按钮 `aria-label`、当前页 `aria-current="true"` 指示。将组件挂载到 `src/app/page.tsx` 的 `public-hero` 区域下方，保留既有筛选/列表/CTA 路径不变（不改“查看详情与票规”语义）。在 `tests/app-shell.test.ts` 增加源码断言：首页存在 `HeroDealCarousel` 引入、`首页特价轮换` 标识、`暂停轮播` 文案常量。</action>
  <acceptance_criteria>
    - `src/components/home/HeroDealCarousel.tsx` 包含 `aria-label="首页特价轮换"`
    - `src/components/home/HeroDealCarousel.tsx` 包含 `4500` 自动轮播间隔值
    - `src/components/home/HeroDealCarousel.tsx` 包含 `暂停轮播` 与 `继续轮播` 文案
    - `src/app/page.tsx` 包含 `HeroDealCarousel` 组件引用
    - `tests/app-shell.test.ts` 包含上述轮换组件合同断言
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/app-shell.test.ts</automated>
  </verify>
  <done>首页具备可控轮换卡片区，移动端/桌面端均可操作，自动播放在可访问性条件下可控且可关闭。</done>
</task>

<task type="auto">
  <name>Task 2: 强化首页底色与布局层级，并锁定动效一致性回归测试</name>
  <files>src/styles/globals.css, tests/design-contract.test.ts</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md, src/styles/globals.css, tests/design-contract.test.ts</read_first>
  <action>按 D-05、D-06、D-08 与 UI-UX-Pro-Max 思路优化首页“高级感”基底：在 `globals.css` 新增 `hero-carousel`、`hero-carousel__track`、`hero-carousel__slide`、`hero-carousel__controls` 样式，形成“背景层（低饱和渐变）→内容层（Hero）→交互层（轮换卡片）”三级层级；卡片采用统一圆角 14-16px、柔和阴影与浅描边，不引入外部动画库。交互动效统一使用既有 `180ms` 节奏，轮播过渡不超过 `320ms`，并新增 `@media (prefers-reduced-motion: reduce)` 下禁用自动位移/过渡。为移动端增加 375px 下卡片内边距与文案截断保护，确保不出现横向滚动。更新 `tests/design-contract.test.ts`：断言 `globals.css` 存在 `.hero-carousel`、`prefers-reduced-motion: reduce`、`transition` 节奏与轮播相关 class，防止“氛围改了但不可回归验证”。</action>
  <acceptance_criteria>
    - `src/styles/globals.css` 包含 `.hero-carousel`、`.hero-carousel__slide`、`.hero-carousel__controls`
    - `src/styles/globals.css` 包含 `@media (prefers-reduced-motion: reduce)`
    - `src/styles/globals.css` 中轮播过渡时长不超过 `320ms`
    - `tests/design-contract.test.ts` 包含对上述 class 与 reduced-motion 规则的断言
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/design-contract.test.ts</automated>
  </verify>
  <done>首页底色、层级和动效形成统一高级感表达，并具备可自动回归的合同测试。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 首页数据→轮换组件渲染 | 文案长度与数据时效不稳定，轮换改造不能遮蔽核心决策信息 |
| 用户交互→自动播放控制 | 自动轮播若不可控会造成可访问性与可用性问题 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | D | src/components/home/HeroDealCarousel.tsx | mitigate | 提供显式暂停/播放与手动翻页控制；reduced-motion 默认不自动播放 |
| T-06-12 | I | src/styles/globals.css | mitigate | 保持价格/时效/CTA 视觉优先级，避免背景与装饰覆盖关键信息 |
</threat_model>

<verification>
- `npm run lint`
- `npm test -- tests/app-shell.test.ts tests/design-contract.test.ts`
</verification>

<success_criteria>
- UAT 对“首页氛围感不足”反馈关闭：首页具备明确轮换层级与高级感底色表达
- 轮换卡片满足可访问性（可控播放、reduced-motion）与移动端适配
- 新增样式与交互均由自动化断言覆盖，后续可稳定回归
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-ui-ui-phase-front-design-ui-ux-skills-bug-06-SUMMARY.md`
</output>

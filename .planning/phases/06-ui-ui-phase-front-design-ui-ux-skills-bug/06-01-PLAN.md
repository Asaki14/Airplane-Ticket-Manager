---
phase: 06-ui-ui-phase-front-design-ui-ux-skills-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/styles/globals.css
  - src/app/page.tsx
  - tests/design-contract.test.ts
  - tests/app-shell.test.ts
autonomous: true
requirements:
  - PREF-03
must_haves:
  truths:
    - "用户在首页能感知统一的轻春日视觉氛围且不影响信息阅读"
    - "公共壳子在移动端与桌面端都保持统一间距和交互触达"
    - "关键按钮与焦点态具备一致可见的交互反馈"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "春日主题 token 与字体/间距基线"
      contains: "--color-dominant"
    - path: "src/styles/globals.css"
      provides: "统一卡片骨架、壳子节奏、动效与焦点态规则"
      contains: ".deal-card-shell"
    - path: "src/app/page.tsx"
      provides: "首页 Hero 与空状态文案符合 UI-SPEC"
      contains: "当前筛选下暂无可用特价，试试放宽条件或切换出发地/日期窗口。"
  key_links:
    - from: "src/styles/tokens.css"
      to: "src/styles/globals.css"
      via: "CSS 变量消费"
      pattern: "var\(--color-accent\)"
    - from: "src/styles/globals.css"
      to: "src/app/page.tsx"
      via: "className 绑定"
      pattern: "public-hero|public-shell|feed-empty"
---

<objective>
建立 Phase 6 的视觉与布局基础层，先统一 token、壳子节奏、骨架类与首页春日表达，为后续卡片与详情页重排提供稳定契约。

Purpose: 先固化全局设计契约，避免后续组件改造出现样式分叉或反复回退。
Output: 可复用的全局样式基线 + 首页文案/视觉同步 + 基础测试断言。
</objective>

<execution_context>
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-RESEARCH.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md

<interfaces>
From src/app/page.tsx:
```tsx
export default async function HomePage({ searchParams }: { searchParams: Promise<SearchParams> })
```

From src/styles/tokens.css:
```css
:root {
  --color-dominant: #F7F4ED;
  --color-secondary: #E5EDE8;
  --color-accent: #0B7A75;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化春日 token 与全局骨架样式基线</name>
  <files>src/styles/tokens.css, src/styles/globals.css</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md, .planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md, src/styles/tokens.css, src/styles/globals.css</read_first>
  <action>按 D-02、D-05、D-06、D-08 在 `globals.css` 增加统一骨架类并写入明确值：新增 `.deal-card-shell`, `.deal-card-shell__header`, `.deal-card-shell__body`, `.deal-card-shell__footer`（header/body/footer 三段式），`.deal-card-shell` 需含 `display:grid; grid-template-rows:auto 1fr auto; min-height: 320px; border-radius: 14px; border: 1px solid #cdd8d1;`。新增桌面与移动统一节奏类：卡片/按钮 hover 与 focus 反馈使用 `transition: transform 180ms ease, box-shadow 180ms ease, background-color 180ms ease`，禁止出现 `animation-duration` 大于 `320ms`（满足 D-08 “中高强度但非长动画”）。保留 UI-SPEC 颜色与 44px 触达下限，不新增第三方样式依赖。</action>
  <acceptance_criteria>
    - `src/styles/globals.css` 包含 `.deal-card-shell` 与 `grid-template-rows:auto 1fr auto`
    - `src/styles/globals.css` 包含 `min-height: 320px`
    - `src/styles/globals.css` 包含 `transition: transform 180ms ease, box-shadow 180ms ease, background-color 180ms ease`
    - `src/styles/globals.css` 中不存在 `animation-duration: 400ms` 或更长显式值
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>全局样式中出现统一卡片骨架、动效与焦点规则，后续页面可直接复用。</done>
</task>

<task type="auto">
  <name>Task 2: 同步首页春日表达与空态文案合同</name>
  <files>src/app/page.tsx, tests/design-contract.test.ts, tests/app-shell.test.ts</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md, src/app/page.tsx, tests/design-contract.test.ts, tests/app-shell.test.ts</read_first>
  <action>按 D-05、D-06 将首页 Hero 与空态文案对齐 UI-SPEC：`src/app/page.tsx` 中空状态文案替换为 `当前筛选下暂无可用特价，试试放宽条件或切换出发地/日期窗口。`；保留主 CTA `查看详情与票规`。为 `tests/design-contract.test.ts` 与 `tests/app-shell.test.ts` 增加断言：检查首页仍引用 `var(--color-` token、仍含 `public-hero` 与主导航，同时断言空状态新文案存在，避免回退到旧文案。</action>
  <acceptance_criteria>
    - `src/app/page.tsx` 包含精确文案 `当前筛选下暂无可用特价，试试放宽条件或切换出发地/日期窗口。`
    - `tests/design-contract.test.ts` 或 `tests/app-shell.test.ts` 包含上述空态文案断言
    - `src/app/page.tsx` 仍包含 `public-hero` 与 `主导航`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/design-contract.test.ts tests/app-shell.test.ts</automated>
  </verify>
  <done>首页视觉语气和文案与 UI 合同一致，且有自动化断言防回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户筛选输入→首页渲染 | 不可信输入触发展示层，样式重构不得影响可读性与信息层级 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | src/app/page.tsx | mitigate | 固定空态与 CTA 文案，保持 freshness/决策信息可读层级，增加文案断言测试 |
| T-06-02 | D | src/styles/globals.css | mitigate | 限制动画时长与位移，禁止长时动画与重视差，保留 44px 触达标准 |
</threat_model>

<verification>
- `npm run lint`
- `npm test -- tests/design-contract.test.ts tests/app-shell.test.ts`
</verification>

<success_criteria>
- 全局样式具备可复用的统一骨架类与受控动效
- 首页空态与视觉语气符合 UI-SPEC，且测试可自动捕获回退
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-ui-ui-phase-front-design-ui-ux-skills-bug-01-SUMMARY.md`
</output>

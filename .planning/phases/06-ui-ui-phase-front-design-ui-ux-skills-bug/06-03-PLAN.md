---
phase: 06-ui-ui-phase-front-design-ui-ux-skills-bug
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/app/deals/[id]/page.tsx
  - src/styles/globals.css
  - tests/deal-detail.test.ts
  - tests/design-contract.test.ts
autonomous: true
requirements:
  - PREF-03
must_haves:
  truths:
    - "用户在详情页不再看到高而窄的三列卡片布局"
    - "用户能在移动端单列、桌面端双列下快速读取来源时效/票规/价值解释"
    - "长票规内容通过短卡与折叠策略可控呈现"
  artifacts:
    - path: "src/app/deals/[id]/page.tsx"
      provides: "详情页双列分组结构与票规短卡"
      contains: "detail-grid--primary"
    - path: "src/styles/globals.css"
      provides: "详情页断点重排样式（1024+ 双列）"
      contains: ".detail-grid--primary"
    - path: "tests/deal-detail.test.ts"
      provides: "详情页关键信息区块与 CTA 存在断言"
      contains: "去来源页继续购买"
  key_links:
    - from: "src/app/deals/[id]/page.tsx"
      to: "src/styles/globals.css"
      via: "detail-grid 变体 class"
      pattern: "detail-grid--primary|detail-grid--rules"
    - from: "src/app/deals/[id]/page.tsx"
      to: "tests/deal-detail.test.ts"
      via: "区块标题文案断言"
      pattern: "来源与时效|票规翻译|为什么值得买"
---

<objective>
修复详情页核心布局缺陷：将当前导致“高窄卡片”的三列布局重排为移动端单列、桌面端双列分组，并为票规区引入短卡化与可控折叠。

Purpose: 解决 Phase 6 最关键显示 bug，提升详情页决策效率与可读性。
Output: 详情页结构改造 + 断点样式重排 + 回归测试更新。
</objective>

<execution_context>
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/Airplane-Ticket-Manager/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-RESEARCH.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md
@.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-02-PLAN.md

<interfaces>
From src/app/deals/[id]/page.tsx:
```tsx
export default async function DealDetailPage({ params }: DealDetailPageProps)
```

Current sections to preserve semantically:
```tsx
<section aria-label="来源与时效">...</section>
<section aria-label="票规翻译">...</section>
<section aria-label="值得买解释">...</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重排详情页信息分组为单列/双列并修复高窄问题</name>
  <files>src/app/deals/[id]/page.tsx, src/styles/globals.css</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md, src/app/deals/[id]/page.tsx, src/styles/globals.css</read_first>
  <action>按 D-03 将详情页布局从“通用 detail-grid 三列”改为分组变体：在 `page.tsx` 引入 `detail-grid detail-grid--primary`（来源与时效 + 价格与舱位）和 `detail-grid detail-grid--rules`（行李/退改/经停限制）两个分区；在 `globals.css` 中设置 `detail-grid--primary`、`detail-grid--rules` 默认单列，`@media (min-width: 1024px)` 时均为 `repeat(2, minmax(0, 1fr))`，并删除或覆盖详情页场景下的 `detail-grid` 三列规则，确保详情页不再使用 3 列。每个详情卡使用统一骨架类并设置 `min-height: 220px` 防止窄高拉伸。</action>
  <acceptance_criteria>
    - `src/app/deals/[id]/page.tsx` 包含 `detail-grid--primary` 与 `detail-grid--rules`
    - `src/styles/globals.css` 在 `@media (min-width: 1024px)` 下为上述类配置 `repeat(2, minmax(0, 1fr))`
    - `src/styles/globals.css` 中详情页场景不存在 `detail-grid` 三列生效路径
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>详情页在桌面端稳定双列、移动端单列，原高窄问题被布局层消除。</done>
</task>

<task type="auto">
  <name>Task 2: 票规短卡化与折叠策略落地并补齐测试</name>
  <files>src/app/deals/[id]/page.tsx, src/styles/globals.css, tests/deal-detail.test.ts, tests/design-contract.test.ts</files>
  <read_first>.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-CONTEXT.md, .planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-UI-SPEC.md, src/app/deals/[id]/page.tsx, tests/deal-detail.test.ts, tests/design-contract.test.ts</read_first>
  <action>按 D-04 在票规区实现“多个短卡 + 长文本折叠”：对 `refundChangeSummary` 与 `restrictions` 使用 `<details><summary>展开完整规则</summary>...</details>` 或等价实现，默认仅展示摘要首段；为规则卡添加统一类 `rule-card`，限制摘要为 3 行，展开后显示完整文本。保留 `为什么值得买` 区块与 `去来源页继续购买` CTA，不改业务字段。更新 `tests/deal-detail.test.ts` 断言新增折叠文案（如 `展开完整规则`）与三大区块仍存在；更新 `tests/design-contract.test.ts` 增加详情页使用新变体类名的源码断言，防止回退到旧布局。</action>
  <acceptance_criteria>
    - `src/app/deals/[id]/page.tsx` 包含 `展开完整规则`（或同义固定文案）与 `<details>` 结构
    - `tests/deal-detail.test.ts` 包含对折叠文案或 details 结构的断言
    - `tests/design-contract.test.ts` 包含 `detail-grid--primary` 或 `detail-grid--rules` 断言
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/deal-detail.test.ts tests/design-contract.test.ts</automated>
  </verify>
  <done>票规内容默认短卡化可读，长文本按需展开，详情页关键信息仍完整可达。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deal 详情数据→详情页渲染 | 外部/后台来源文案长度不可控，必须防止布局破坏与关键信息淹没 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | I | src/app/deals/[id]/page.tsx | mitigate | 固定关键区块顺序与标题，票规长文本默认折叠，保证决策信息先可见 |
| T-06-06 | T | src/app/deals/[id]/page.tsx | mitigate | 保留 `target="_blank"` + `rel="noreferrer"` 外链属性，测试防止链接属性回退 |
</threat_model>

<verification>
- `npm run lint`
- `npm test -- tests/deal-detail.test.ts tests/design-contract.test.ts`
</verification>

<success_criteria>
- 详情页桌面端双列/移动端单列布局生效且不再出现高窄卡片
- 票规短卡与折叠策略落地，关键决策信息始终先展示
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ui-phase-front-design-ui-ux-skills-bug/06-ui-ui-phase-front-design-ui-ux-skills-bug-03-SUMMARY.md`
</output>

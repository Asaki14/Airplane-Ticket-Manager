---
phase: 01-foundation-deal-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/admin/page.tsx
  - src/middleware.ts
  - src/styles/tokens.css
  - src/styles/globals.css
autonomous: true
requirements:
  - PREF-03
must_haves:
  truths:
    - "用户在移动端和桌面端都能进入公开首页并完成基础导航"
    - "同一个应用内存在受限的 /admin 入口，且不引入完整管理员认证系统"
    - "public 与 admin 共享设计系统，但视觉语气不同（public 更 travel，admin 更 operational）"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "public 首页壳子与导航入口"
    - path: "src/app/admin/page.tsx"
      provides: "admin 首页壳子"
    - path: "src/middleware.ts"
      provides: "轻量 /admin 访问守门"
    - path: "src/styles/tokens.css"
      provides: "UI-SPEC 颜色/间距/排版 token"
  key_links:
    - from: "src/middleware.ts"
      to: "src/app/admin/page.tsx"
      via: "matcher + 请求拦截"
      pattern: "matcher.*admin"
    - from: "src/styles/tokens.css"
      to: "src/app/page.tsx"
      via: "全局样式变量"
      pattern: "var\\(--"
---

<objective>
交付 Phase 1 的单应用基础壳子：public 与 /admin 双表面、轻量受限入口、以及可复用的响应式视觉 token。

Purpose: 先把 D-01/D-02/D-03 与 D-11/D-12/D-13 对应的“骨架约束”落地，后续 CMS 与 feed 才能在统一结构上扩展。
Output: 可运行的 Next.js 壳子页面、/admin 守门中间件、UI token 与全局样式。
</objective>

<execution_context>
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-deal-cms/01-CONTEXT.md
@.planning/phases/01-foundation-deal-cms/01-RESEARCH.md
@.planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立单应用 public/admin 路由壳子与响应式基础</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-CONTEXT.md
    .planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
    src/app/layout.tsx
    src/app/page.tsx
  </read_first>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/admin/page.tsx, src/styles/tokens.css, src/styles/globals.css</files>
  <action>按 D-01 在单一 Next.js App Router 中创建 public（`/`）和 admin（`/admin`）两套页面壳子；按 D-11/D-12 将 public 首页首屏设为 travel 氛围优先（含 hero/氛围色带 + 功能导航），按 D-13 将 admin 壳子保留同 token 但使用更克制背景和更高信息密度。严格使用 UI-SPEC 的断点（0-767,768-1023,1024+）与 spacing/color/typography token。</action>
  <acceptance_criteria>
    - `src/app/page.tsx` 包含 public 首页 hero 区块和导航区域
    - `src/app/admin/page.tsx` 存在且包含 "admin" 页面标题与运营入口文案
    - `src/styles/tokens.css` 包含 `--color-dominant: #F7F4ED`、`--color-secondary: #E5EDE8`、`--color-accent: #0B7A75`
    - `src/styles/globals.css` 定义 mobile-first 样式并包含 1024px 以上布局规则
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run test:quick -- app-shell</automated>
  </verify>
  <done>public 与 admin 壳子页面均可渲染；移动端与桌面端布局规则可从样式文件中直接验证。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 /admin 轻量受限入口（不引入完整认证系统）</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-CONTEXT.md
    src/middleware.ts
    src/app/admin/page.tsx
  </read_first>
  <files>src/middleware.ts, src/app/admin/page.tsx</files>
  <action>按 D-02 与 D-03 实现轻量 admin gate：仅拦截 `/admin` 路径并基于环境变量口令/BASIC 风格校验通过后放行；禁止引入完整管理员认证系统（如完整用户模型、会话体系、RBAC）。在 admin 页显式标注“内部运营入口（MVP）”。</action>
  <acceptance_criteria>
    - `src/middleware.ts` 包含 `/admin` matcher
    - `src/middleware.ts` 含环境变量检查（如 `ADMIN_GATE_*`）
    - 代码库不存在新建的完整 auth 模块（如 `src/app/api/auth/*`）
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- admin-gate</automated>
  </verify>
  <done>未通过 gate 的请求无法直接访问 `/admin`；且实现保持“轻量守门”而非完整认证系统。</done>
</task>

<task type="auto">
  <name>Task 3: 固化 public/admin 共用设计系统与语气分层</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
    src/styles/tokens.css
    src/styles/globals.css
    src/app/page.tsx
    src/app/admin/page.tsx
  </read_first>
  <files>src/styles/tokens.css, src/styles/globals.css, src/app/page.tsx, src/app/admin/page.tsx</files>
  <action>落实 D-11/D-13：public 与 admin 使用同一 token 文件（颜色、字号、间距、focus ring），但 public 保留 travel-oriented 背景氛围，admin 去除装饰并强化运营操作可读性；保证交互焦点样式为 2px accent ring + 2px offset（UI-SPEC 交互约束）。</action>
  <acceptance_criteria>
    - `src/styles/globals.css` 包含统一 focus 样式（2px ring + 2px offset）
    - `src/app/page.tsx` 与 `src/app/admin/page.tsx` 均使用同一 token 变量前缀
    - admin 页面不包含 public hero 装饰区块
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- design-contract</automated>
  </verify>
  <done>视觉系统统一且语气分层明确；后续 CMS 与 feed 页面可直接复用同一 token 体系。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/admin | 未受信任用户尝试访问内部入口 |
| browser→middleware | Header/Cookie 可被伪造，需要服务端校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | `src/middleware.ts` admin gate | mitigate | 对 `/admin` 强制环境变量凭证校验，未通过即拒绝 |
| T-01-02 | I | public/admin 路由 | mitigate | admin 页面不泄露敏感运营数据，仅展示壳子与入口文案 |
| T-01-03 | D | middleware path matching | accept | 仅 MVP 内部流量，DoS 风险低；后续阶段再加入限流 |
</threat_model>

<verification>
- `npm run lint`
- `npm run test:quick -- app-shell`
- `npm run test:quick -- admin-gate`
</verification>

<success_criteria>
- 单应用内 public 与 `/admin` 双表面可用（D-01, D-02）
- `/admin` 为轻量受限入口且未引入完整认证系统（D-03）
- 移动端/桌面端壳子与设计 token 就绪，满足 PREF-03 基础要求
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deal-cms/01-01-SUMMARY.md`
</output>

---
phase: 01-foundation-deal-cms
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/middleware.ts
  - tests/admin-gate.test.ts
  - tests/admin-gate-auth-success.test.ts
  - scripts/check-admin-gate-env.mjs
  - package.json
  - .planning/phases/01-foundation-deal-cms/01-UAT.md
autonomous: true
requirements:
  - OPS-01
  - PREF-03
gap_closure: true
must_haves:
  truths:
    - "访问 /admin/deals 与 /admin/deals/{id} 时，未认证或错误认证都会触发 Basic challenge，而不是静默失败"
    - "提供正确 Basic 凭证后，可成功访问 /admin/deals 与 /admin/deals/{id}（对应 UAT test 3/4 正向路径）"
    - "当 ADMIN_GATE_USERNAME/ADMIN_GATE_PASSWORD 缺失或为空时，系统给出可观测的配置错误提示，且不改变 Basic gate 契约"
    - "本地启动与 UAT 步骤会在执行前校验 admin gate 环境变量，避免再次出现‘无弹窗无法进入后台’"
  artifacts:
    - path: "src/middleware.ts"
      provides: "admin gate 在缺失配置时仍返回 401 + WWW-Authenticate，并附带可观测提示"
      exports: ["middleware", "config"]
    - path: "scripts/check-admin-gate-env.mjs"
      provides: "启动前 ADMIN_GATE 环境变量非空校验"
    - path: "tests/admin-gate.test.ts"
      provides: "admin gate 合同回归测试（包含缺失配置分支）"
    - path: "tests/admin-gate-auth-success.test.ts"
      provides: "正向鉴权测试：正确 Basic 凭证可访问 /admin/deals 与 /admin/deals/{id}"
    - path: ".planning/phases/01-foundation-deal-cms/01-UAT.md"
      provides: "UAT 前置步骤：admin gate 配置校验与访问契约说明"
  key_links:
    - from: "scripts/check-admin-gate-env.mjs"
      to: "package.json"
      via: "npm scripts 在 dev/UAT 前执行"
      pattern: "check:admin-gate-env"
    - from: "src/middleware.ts"
      to: "/admin/:path*"
      via: "middleware matcher"
      pattern: "matcher: \['/admin/:path\*'\]"
    - from: "tests/admin-gate.test.ts"
      to: "src/middleware.ts"
      via: "合同断言"
      pattern: "WWW-Authenticate|ADMIN_GATE"
    - from: "tests/admin-gate-auth-success.test.ts"
      to: "src/middleware.ts"
      via: "正向路径断言"
      pattern: "/admin/deals|/admin/deals/.+|authorization"
---

<objective>
修复 Phase 1 UAT 的 admin 入口不可达问题：保持 D-02/D-03 的 Basic gate 轻量契约不变，补齐配置缺失时的可观测性与启动前校验，确保 `/admin/deals` 与 `/admin/deals/{id}` 可稳定验收。

Purpose: 关闭 UAT test 3 与 test 4 的同源 root cause，避免在 MVP 范围内误引入应用内登录体系。
Output: middleware 修复、环境变量预检脚本、回归测试、UAT 操作指引更新。
</objective>

<execution_context>
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deal-cms/01-CONTEXT.md
@.planning/phases/01-foundation-deal-cms/01-UAT.md
@.planning/debug/admin-deals-no-basic-auth.md
@.planning/debug/admin-deals-auth-blocked.md
@.planning/phases/01-foundation-deal-cms/01-01-SUMMARY.md
@.planning/phases/01-foundation-deal-cms/01-02-SUMMARY.md

<interfaces>
From src/middleware.ts:
```ts
export function middleware(request: NextRequest): NextResponse;
export const config = { matcher: ['/admin/:path*'] };
```

From tests/admin-gate.test.ts:
```ts
describe('admin-gate', () => {
  it('middleware matches /admin path and checks ADMIN_GATE env vars', ...)
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 middleware 缺失配置分支，保持 Basic gate 契约（D-02, D-03）</name>
  <files>src/middleware.ts, tests/admin-gate.test.ts, tests/admin-gate-auth-success.test.ts</files>
  <behavior>
    - Test 1: `/admin` 在无 Authorization 头时返回 401 且包含 `WWW-Authenticate: Basic realm="Admin Gate"`
    - Test 2: 当 `ADMIN_GATE_USERNAME` 或 `ADMIN_GATE_PASSWORD` 缺失/空字符串时，仍返回 401 + challenge，而非 503
    - Test 3: 缺失配置分支会返回可观测提示（响应体或响应头包含 misconfigured 信息），便于快速定位
    - Test 4: 设置正确 `ADMIN_GATE_USERNAME/ADMIN_GATE_PASSWORD` 且请求携带正确 Basic Authorization 时，`/admin/deals` 返回放行结果（next）
    - Test 5: 设置正确 `ADMIN_GATE_USERNAME/ADMIN_GATE_PASSWORD` 且请求携带正确 Basic Authorization 时，`/admin/deals/{id}` 返回放行结果（next）
  </behavior>
  <action>重构 `src/middleware.ts`：将当前“env 缺失直接 503”改为“统一 challenge 401 + WWW-Authenticate”，并在响应中增加配置错误可观测标记。严格保持 Basic Auth gate，不新增 session/cookie/login 路径（per D-03）；`matcher` 继续限定在 `/admin/:path*`（per D-02）。同步增强 `tests/admin-gate.test.ts` 覆盖缺失配置分支与 challenge 头断言；新增 `tests/admin-gate-auth-success.test.ts`，显式断言正确凭证可访问 `/admin/deals` 与 `/admin/deals/{id}`（对齐 UAT test 3/4 正向验收）。</action>
  <verify>
    <automated>npm run test:quick -- admin-gate && npm run test:quick -- admin-gate-auth-success</automated>
  </verify>
  <done>即使配置缺失，浏览器仍会触发 Basic Auth challenge；且自动化测试已证明正确凭证可进入 `/admin/deals` 与 `/admin/deals/{id}`，直接覆盖 UAT test 3/4 正向路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 admin gate 启动前预检并接入 npm 脚本（覆盖 gap missing #1）</name>
  <files>scripts/check-admin-gate-env.mjs, package.json, tests/admin-gate.test.ts, tests/admin-gate-auth-success.test.ts</files>
  <behavior>
    - Test 1: 提供非空 `ADMIN_GATE_USERNAME/ADMIN_GATE_PASSWORD` 时，预检脚本退出码为 0
    - Test 2: 任一变量缺失或空值时，预检脚本退出码非 0 且输出缺失项
    - Test 3: `npm run dev` 先执行预检再启动 Next dev
  </behavior>
  <action>新增 `scripts/check-admin-gate-env.mjs`，用于强制校验 `ADMIN_GATE_USERNAME` 与 `ADMIN_GATE_PASSWORD` 非空；在 `package.json` 增加 `check:admin-gate-env` 并将 `dev` 脚本串联为“先校验后启动”。不得引入新认证机制，只做配置就绪检查。补充测试覆盖脚本行为（可在现有 `tests/admin-gate.test.ts` 增加用例或拆分新测试文件）。</action>
  <verify>
    <automated>ADMIN_GATE_USERNAME=uat ADMIN_GATE_PASSWORD=secret node scripts/check-admin-gate-env.mjs && npm run test:quick -- admin-gate && npm run test:quick -- admin-gate-auth-success</automated>
  </verify>
  <done>本地启动流程可在进入 UAT 前阻断空配置；同类误配置不再以“无弹窗”形式延后暴露。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 Phase 1 UAT 操作契约与复验命令（覆盖 gap missing #2）</name>
  <files>.planning/phases/01-foundation-deal-cms/01-UAT.md</files>
  <action>在 `01-UAT.md` 中追加“admin 访问契约”与“执行前检查”段落：明确 Phase 1 的 `/admin` 访问方式是 Basic Auth（非应用内登录，per D-03）；给出标准启动/复验步骤（先运行 `check:admin-gate-env`，再访问 `/admin/deals` 与 `/admin/deals/{id}`）。仅更新与当前两条 gap 相关内容，不扩展到 feed/DealCard 等无关功能。</action>
  <verify>
    <automated>npm run lint && npm run test:quick -- admin-gate && npm run test:quick -- admin-gate-auth-success</automated>
  </verify>
  <done>UAT 文档已能指导执行者稳定复现并验证 admin gate；并明确“正确凭证可进入 `/admin/deals` 与 `/admin/deals/{id}`”为 test 3/4 验收标准。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→`/admin/:path*` middleware | 未受信任请求进入后台入口守门逻辑 |
| local shell→dev startup scripts | 本地环境变量错误会影响鉴权可达性与误判风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | S | `src/middleware.ts` | mitigate | 严格 Basic challenge + 凭证校验，拒绝缺失/错误 Authorization |
| T-01-11 | D | `src/middleware.ts` | mitigate | 配置缺失不再走 503 静默路径，统一 challenge 并提供可观测错误标记 |
| T-01-12 | R | `01-UAT.md` + `scripts/check-admin-gate-env.mjs` | mitigate | 文档化固定复验步骤并执行预检，降低“口头配置正确但实际为空”争议 |
</threat_model>

<verification>
- `ADMIN_GATE_USERNAME=uat ADMIN_GATE_PASSWORD=secret node scripts/check-admin-gate-env.mjs`
- `npm run test:quick -- admin-gate`
- `npm run test:quick -- admin-gate-auth-success`
- `npm run lint`
</verification>

<success_criteria>
- UAT test 3 对应真值恢复：`/admin/deals` 在未认证时触发 Basic challenge，认证后可进入列表
- UAT test 4 对应真值恢复：`/admin/deals/{id}` 走同一 gate 契约并可进入编辑页
- 自动化测试命令可直接证明正向路径：正确 Basic 凭证分别访问 `/admin/deals` 与 `/admin/deals/{id}` 均放行
- 启动与验收流程对 `ADMIN_GATE_*` 具备前置失败保护，且不引入超出 MVP 的完整登录系统
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deal-cms/01-04-SUMMARY.md`
</output>

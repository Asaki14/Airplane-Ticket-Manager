---
phase: 01-foundation-deal-cms
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/api/deals/feed/route.ts
  - src/lib/deals/feed-query.ts
  - src/app/page.tsx
  - src/components/deals/DealCard.tsx
autonomous: true
requirements:
  - OPS-02
  - PREF-03
must_haves:
  truths:
    - "公开 feed 只展示未过期且已发布的 deal"
    - "过期 deal 在 admin 保留可查，但在 public 自动消失"
    - "public 卡片主价格为 headline lowest，reference total-cost 作为辅助信息"
  artifacts:
    - path: "src/app/api/deals/feed/route.ts"
      provides: "public feed 查询入口"
      exports: ["GET"]
    - path: "src/lib/deals/feed-query.ts"
      provides: "发布态+未过期过滤逻辑"
    - path: "src/components/deals/DealCard.tsx"
      provides: "价格与 freshness 展示组件"
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/deals/feed"
      via: "首页数据请求"
      pattern: "fetch.*api/deals/feed"
    - from: "src/lib/deals/feed-query.ts"
      to: "deals collection"
      via: "status + expiresAt 条件"
      pattern: "status.*published.*expiresAt"
---

<objective>
打通公开 feed 的读取与时效过滤，让已过期 deal 从 public 自动消失并保持价格展示决策一致。

Purpose: 完成 OPS-02 并补齐 D-09/D-14/D-15 在用户可见面的最终落地。
Output: feed API、过期过滤查询、首页卡片展示与 freshness 元数据顺序。
</objective>

<execution_context>
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-deal-cms/01-CONTEXT.md
@.planning/phases/01-foundation-deal-cms/01-RESEARCH.md
@.planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
@.planning/phases/01-foundation-deal-cms/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 public feed 查询与过期自动隐藏逻辑</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-CONTEXT.md
    src/app/api/deals/feed/route.ts
    src/lib/deals/feed-query.ts
    src/collections/Deals.ts
  </read_first>
  <files>src/lib/deals/feed-query.ts, src/app/api/deals/feed/route.ts</files>
  <behavior>
    - Test 1: status=draft 的记录不会出现在 public feed
    - Test 2: status=published 但 expiresAt <= now 的记录不会出现在 public feed
    - Test 3: status=published 且 expiresAt > now 的记录会返回
  </behavior>
  <action>按 D-09 实现读取侧硬过滤：feed 查询必须同时满足 `status=published` 与 `expiresAt > now()`；API 层返回结构化 freshness 字段（publishedAt/updatedAt/expiresAt），用于前端固定顺序展示。明确保留 admin 侧历史记录，不在此步骤删除或硬归档记录。</action>
  <acceptance_criteria>
    - `src/lib/deals/feed-query.ts` 包含 status 与 expiresAt 双条件过滤
    - `src/app/api/deals/feed/route.ts` 导出 `GET`
    - API 响应字段包含 `publishedAt`, `updatedAt`, `expiresAt`
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- feed-expiry</automated>
  </verify>
  <done>public feed 查询天然排除过期与未发布记录，且 freshness 字段完整返回。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 public DealCard 价格与 freshness 展示规则</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-CONTEXT.md
    .planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
    src/app/page.tsx
    src/components/deals/DealCard.tsx
  </read_first>
  <files>src/components/deals/DealCard.tsx, src/app/page.tsx</files>
  <action>按 D-14 将卡片主价格固定为 `headlinePrice`；按 D-15 在价格区域或辅助说明位展示 `referenceTotalPrice`（标记为参考总成本，不替代主价格）。按 UI-SPEC 固定 freshness 顺序为 `发布时间 -> 更新时间 -> 失效时间`，并保持 mobile-first 卡片布局。</action>
  <acceptance_criteria>
    - `src/components/deals/DealCard.tsx` 主显示字段使用 `headlinePrice`
    - 同组件包含 `referenceTotalPrice` 辅助文案
    - 同组件中 freshness 渲染顺序为 publishedAt→updatedAt→expiresAt
    - `src/app/page.tsx` 调用 feed API 并渲染 DealCard 列表
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- deal-card-pricing && npm run test:quick -- freshness-order</automated>
  </verify>
  <done>public 首页可读取真实 feed 并按既定价格与时效规则展示 deal 卡片。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public client→feed API | 任何外部请求都可能传入恶意参数或探测数据 |
| feed query→data store | 过滤条件错误会导致过期或非公开数据泄露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | `src/app/api/deals/feed/route.ts` | mitigate | API 仅返回公开所需字段，排除内部运营字段 |
| T-01-08 | T | `src/lib/deals/feed-query.ts` | mitigate | 强制 status+expiresAt 双条件过滤，防止篡改查询导致泄露 |
| T-01-09 | E | public card price rendering | accept | 价格展示本身不授予权限；低风险，后续阶段补充签名来源校验 |
</threat_model>

<verification>
- `npm run test:quick -- feed-expiry`
- `npm run test:quick -- deal-card-pricing`
- `npm run test:quick -- freshness-order`
</verification>

<success_criteria>
- 过期 deal 自动从 public 列表消失且仍可在 admin 保留（OPS-02, D-09）
- 公开卡片主价格与辅助价格角色清晰（D-14, D-15）
- 首页在移动端和桌面端都可阅读核心 deal 信息（PREF-03）
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deal-cms/01-03-SUMMARY.md`
</output>

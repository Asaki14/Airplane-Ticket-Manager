---
phase: 01-foundation-deal-cms
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/payload.config.ts
  - src/collections/Deals.ts
  - src/app/admin/deals/page.tsx
  - src/app/admin/deals/[id]/page.tsx
  - src/lib/deals/validation.ts
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "运营可以创建、编辑、发布、归档 deal"
    - "deal 字段完整覆盖结构化票价、时窗、规则摘要与时效元数据"
    - "发布动作为单条手动控制，不支持批量发布"
  artifacts:
    - path: "src/collections/Deals.ts"
      provides: "Deal schema 与生命周期字段"
    - path: "src/app/admin/deals/page.tsx"
      provides: "admin deal 列表与单条操作入口"
    - path: "src/app/admin/deals/[id]/page.tsx"
      provides: "deal 编辑与状态变更界面"
  key_links:
    - from: "src/collections/Deals.ts"
      to: "src/app/admin/deals/[id]/page.tsx"
      via: "字段定义驱动表单"
      pattern: "headlinePrice|referenceTotalPrice|travelWindowLabel"
    - from: "src/app/admin/deals/page.tsx"
      to: "src/app/admin/deals/[id]/page.tsx"
      via: "单条编辑导航"
      pattern: "href.*admin/deals"
---

<objective>
搭建 Deal CMS 的核心数据模型与后台 CRUD 流程，保证运营可完整管理 deal 生命周期。

Purpose: 实现 OPS-01，并把 D-04~D-10 的业务语义固化在 schema 与后台交互中。
Output: Payload Deal collection、admin 列表/编辑页、状态流转与字段校验。
</objective>

<execution_context>
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/wangyao/Desktop/美团AI Coding/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-deal-cms/01-CONTEXT.md
@.planning/phases/01-foundation-deal-cms/01-RESEARCH.md
@.planning/phases/01-foundation-deal-cms/01-VALIDATION.md
@.planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
@.planning/phases/01-foundation-deal-cms/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 Deal collection 与生命周期/字段约束</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-CONTEXT.md
    src/payload.config.ts
    src/collections/Deals.ts
    src/lib/deals/validation.ts
  </read_first>
  <files>src/payload.config.ts, src/collections/Deals.ts, src/lib/deals/validation.ts</files>
  <behavior>
    - Test 1: 创建 deal 时缺少必填结构化字段（title/departureCity/destination/headlinePrice/expiresAt）应被拒绝
    - Test 2: status 仅允许 draft/published/expired（D-08）
    - Test 3: 同时支持 `headlinePrice` 与 `referenceTotalPrice`（D-06）
    - Test 4: 同时支持 `travelStartDate`/`travelEndDate` 与 `travelWindowLabel`（D-07）
  </behavior>
  <action>按 D-04 把 `deals` 定义为“成品推荐记录”集合而非原始票价行；按 D-05 完整实现字段集：title、departure city、destination、headline price、currency、trip type、travel window（结构化+label）、airline、direct/transfer、baggage、refund/change、source link、publish/update/expiry time、recommendation copy、value tags；按 D-08 固化状态枚举 draft/published/expired。</action>
  <acceptance_criteria>
    - `src/collections/Deals.ts` 包含 status 枚举值 `draft|published|expired`
    - 同文件同时出现 `headlinePrice` 与 `referenceTotalPrice`
    - 同文件同时出现 `travelStartDate`、`travelEndDate`、`travelWindowLabel`
    - `src/payload.config.ts` 已注册 `deals` collection
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- deal-schema</automated>
  </verify>
  <done>Deal schema 满足运营录入所需字段与状态语义，且具备可测试的输入约束。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 admin deal 列表/编辑与单条手动发布流程</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-CONTEXT.md
    .planning/phases/01-foundation-deal-cms/01-UI-SPEC.md
    src/app/admin/deals/page.tsx
    src/app/admin/deals/[id]/page.tsx
    src/collections/Deals.ts
  </read_first>
  <files>src/app/admin/deals/page.tsx, src/app/admin/deals/[id]/page.tsx, src/collections/Deals.ts</files>
  <action>实现运营 CRUD 路径：列表页支持查看/筛选状态与进入编辑；编辑页支持新建、保存、归档、发布。按 D-10 仅提供单条记录发布按钮（禁止批量发布入口）。按 UI-SPEC：移动端列表用卡片、桌面用表格；表单移动端单列、桌面 2 列分组（fare basics / travel timing / rule summary / source+freshness / recommendation）。</action>
  <acceptance_criteria>
    - `src/app/admin/deals/page.tsx` 同时包含 mobile card 与 desktop table 渲染分支
    - `src/app/admin/deals/[id]/page.tsx` 包含 `发布` 与 `归档` 单条动作
    - 代码库不存在 batch publish 操作（如 `publishAll`、`bulkPublish`）
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- deal-crud</automated>
  </verify>
  <done>运营可对单条 deal 完整执行创建、编辑、发布、归档；无批量发布路径。</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] 执行 Payload schema migration（验证前必须）</name>
  <read_first>
    .planning/phases/01-foundation-deal-cms/01-RESEARCH.md
    src/collections/Deals.ts
    src/payload.config.ts
  </read_first>
  <files>src/collections/Deals.ts, src/payload.config.ts</files>
  <action>在所有 schema 变更完成后、任何验证命令前执行迁移：`CI=true PAYLOAD_MIGRATING=true npx payload migrate`。若命令受非 TTY 阻塞且无法消除提示，标记该步骤为人工协助并将计划 `autonomous` 更新为 false（当前默认先按自动执行）。</action>
  <acceptance_criteria>
    - 执行日志包含 `payload migrate` 成功完成标记
    - 后续测试基于最新数据库 schema 运行
  </acceptance_criteria>
  <verify>
    <automated>CI=true PAYLOAD_MIGRATING=true npx payload migrate</automated>
  </verify>
  <done>数据库 schema 与 collection 定义同步，避免“类型通过但数据库未更新”的假阳性。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form→CMS API | 运营输入跨越信任边界进入持久化层 |
| schema file→database | 配置变更必须同步数据库，避免运行时不一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `src/collections/Deals.ts` | mitigate | 字段级验证与枚举约束，拒绝非法状态与缺失关键字段 |
| T-01-05 | R | admin 发布/归档动作 | mitigate | 记录 `updatedAt` 并在状态变更时写入审计字段 |
| T-01-06 | D | schema drift | mitigate | 强制 [BLOCKING] 迁移步骤在验证前执行 |
</threat_model>

<verification>
- `CI=true PAYLOAD_MIGRATING=true npx payload migrate`
- `npm run test:quick -- deal-schema`
- `npm run test:quick -- deal-crud`
</verification>

<success_criteria>
- 运营可以 create/edit/publish/archive 一条 deal（OPS-01）
- Deal 模型完整覆盖 D-05 字段族，含双价格与双时窗表达（D-06, D-07）
- 生命周期与发布方式符合 `draft->published->expired` + 单条发布（D-08, D-10）
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deal-cms/01-02-SUMMARY.md`
</output>
